In this contract, the vote function checks whether the voting is open or not, whether or not the voter has already voted and did he cast a valid vote.
The voter list is managed through a mapping
The toggle voting status allows only admin to change the voting status